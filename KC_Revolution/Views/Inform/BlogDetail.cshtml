@model KC_Revolution.Data.Entities.BlogPost
@{
    ViewData["Title"] = Model.Title;
    Layout = "_Layout";
}


<div class="container-fluid">
    <h1 class="text-center page-title">@Model.Title</h1>
    <div class="row">
        <div class="col-sm-12 col-md-6 col-md-offset-3">
            <p style="padding: 15px;">
                @Html.Raw(@Model.LongDescription)
            </p>
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12">
            <hr />
        </div>
    </div>
    <div class="row">
        <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
            @Html.Raw(Model.Body)
        </div>
    </div>
    <div class="row">
        <div class="col-sm-4 col-sm-offset-2">
            <p style="margin: 25px 0 25px;padding: 15px;">
                Created by @Model.FirstName @Model.LastName <br />
                Created on @Model.CreatedDate.ToShortDateString()
            </p>
        </div>
    </div>
</div>
